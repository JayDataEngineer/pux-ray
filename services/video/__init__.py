"""Video services — standalone containers called via HTTP by Forge adapters.

  vace_server.py — DiffSynth-Studio Wan VACE (Wan2.2 VACE-Fun A14B)
                   Bypasses SGLang (VCU inputs unsupported). TeaCache + SageAttention.
"""
