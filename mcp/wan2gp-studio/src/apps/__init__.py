"""MCP App Resources — HTML widgets served by the MCP server.

These are self-contained HTML pages that render as embedded widgets
in the assistant-ui chat. Each app connects back to the MCP server
to call tools and read resources via the bridge protocol.
"""
