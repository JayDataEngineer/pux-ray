apiVersion: v1
kind: Service
metadata:
  name: media-analysis-mcp
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    app: media-analysis-mcp
