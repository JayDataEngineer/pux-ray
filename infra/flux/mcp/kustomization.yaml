apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - web-research-deps.yaml
  - web-research.yaml
  - web-research-worker.yaml
  - equibles-deps.yaml
  - equibles-mcp.yaml
  - equibles-worker.yaml
  - equibles-web.yaml
  # - equibles-scaledobject.yaml  # Requires KEDA — uncomment after KEDA Helm install succeeds
